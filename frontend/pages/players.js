import PleaseSignIn from '../components/PleaseSignIn';

const GameNotesPage = (props) => {
    return( 
        <div>
            <PleaseSignIn>
                <p>Here's where player notes will go.</p>
            </PleaseSignIn>
        </div>
    )
}

export default GameNotesPage;